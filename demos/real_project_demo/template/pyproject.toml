[project]
name = "shopcart-discount-demo"
version = "0.1.0"
description = "A tiny project for miniAgent real-project demo"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q -p no:cacheprovider"
